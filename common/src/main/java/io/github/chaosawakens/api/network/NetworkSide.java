package io.github.chaosawakens.api.network;

/**
 * An object-holder enum representing network sides.
 */
public enum NetworkSide {
    /**
     * C2S (Client to Server). Basically just the server.
     */
    C2S,
    /**
     * S2C (Server to Client). Basically just the client.
     */
    S2C
}
